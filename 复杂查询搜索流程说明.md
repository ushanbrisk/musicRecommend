# 复杂查询搜索流程说明

## 修改概要

已更新 `design.md` 文件，新增 **3.0.3 节：复杂查询的完整搜索流程（示例）**，详细说明了：

1. **如何处理复杂查询**（如"欢快的凯尔特音乐，有小提琴伴奏"）
2. **LLM 在整个系统中的作用**（仅在精排阶段参与）
3. **为什么不在第一阶段使用 LLM 生成 music_feature**

---

## 关键要点

### 1. LLM 参与时机明确化

| 阶段 | LLM 是否参与 | 原因 | 替代方案 |
|------|-------------|------|---------|
| **离线特征生成** | ❌ **不参与（已废弃）** | 成本太高、时间太慢 | 纯 Embedding 方案 |
| **实时粗召回（阶段1）** | ❌ 不参与 | 毫秒级响应 | 向量数据库检索 |
| **实时精排序（阶段2）** | ✅ **参与** | 仅处理 200 首候选 | 理解复杂语义逻辑 |

**结论**：LLM **只在实时精排阶段使用**，不参与离线特征生成和粗召回。

---

### 2. 复杂查询的处理流程

**示例**："欢快的凯尔特音乐，有小提琴伴奏"

```
步骤1：查询预处理
  ↓ 用 Embedding 模型将查询转为向量

步骤2：向量数据库粗召回（阶段1）
  ↓ 在百万歌曲中快速找到最相似的 200 首
  ↓ 毫秒级响应，但不理解复杂逻辑

步骤3：LLM 精排序（阶段2）
  ↓ LLM 分析 200 首候选的完整信息
  ↓ 理解"欢快 AND 凯尔特 AND 小提琴"的复杂逻辑
  ↓ 返回 top 20 + 匹配理由

步骤4：返回最终结果
  ↓ 前端展示结果 + 匹配理由
```

---

### 3. 为什么不在第一阶段用 LLM 生成 music_feature

原设计考虑过用 LLM 生成 `music_features` 表的字段（genre、mood、instruments），但有以下问题：

| 问题 | 说明 | 解决方案 |
|------|------|---------|
| **成本太高** | 百万歌曲 × LLM 调用 ≈ 巨额费用 | 仅用 Embedding 模型 |
| **时间太慢** | 每首 3-5 秒 × 100 万 ≈ 34-58 天 | Embedding 并行处理，1-2 天完成 |
| **不必要** | 向量检索已足够精准 | LLM 仅用于精排 200 首 |

**当前方案**：
- ✅ **离线阶段**：纯 Embedding 方案（成本低、速度快）
- ✅ **实时查询**：LLM 只处理 200 首（成本可控、延迟 1-2 秒）
- ✅ **效果保证**：复杂查询通过 LLM 精排实现

---

### 4. 数据流总结

```
【离线阶段（一次性）】
  songs 表
    ↓
  拼接文本描述（歌名 + 艺术家 + 歌单名 + 评论摘要）
    ↓
  Embedding 模型（非 LLM）
    ↓
  song_embeddings 表（向量数据库）

【实时查询（每次用户搜索）】
  用户输入 query
    ↓
  Embedding 模型转向量
    ↓
  向量数据库检索 top 200（毫秒级）
    ↓
  LLM 精排序（1-2 秒）
    ↓
  返回 top 20
```

---

## 搜索类型对比

| 搜索类型 | 实现方式 | 示例查询 | LLM 参与 |
|----------|----------|----------|---------|
| **基本搜索** | SQL ILIKE 模糊匹配 | "琵琶行"、"周杰伦" | ❌ |
| **语义搜索** | Embedding 向量检索 | "适合工作的轻音乐" | ❌ |
| **复杂搜索** | 向量召回 + LLM 精排 | "欢快的凯尔特音乐，有小提琴伴奏" | ✅ 仅精排 |

---

## 为什么两阶段方案最优

### 只用数据库 ILIKE（传统方案）
```sql
-- ❌ 无法理解语义
SELECT * FROM songs
WHERE song_name ILIKE '%凯尔特%'
   OR artist ILIKE '%小提琴%'  -- 小提琴是乐器，不在 artist 字段
```
**问题**：无法匹配"欢快"、"小提琴伴奏"等语义信息

---

### 只用向量检索
**问题**：
- 可能召回"欢快的古典音乐"（缺少凯尔特）
- 可能召回"凯尔特纯音乐"（缺少小提琴）
- 无法保证同时满足多个条件

---

### 两阶段方案（当前方案）✅
1. **阶段1**：向量检索快速缩小范围（百万 → 200）
2. **阶段2**：LLM 精确理解"欢快 AND 凯尔特 AND 小提琴"

**优势**：
- ✅ 成本可控：LLM 只处理 200 首
- ✅ 延迟可接受：总耗时约 1-2 秒
- ✅ 效果保证：复杂语义理解到位

---

## 实际示例

### 用户输入
> "欢快的凯尔特音乐，有小提琴伴奏"

### 阶段1：向量召回（可能返回的 200 首中）
- ✅ "Celtic Dreams"（凯尔特，欢快，小提琴）← 匹配度高
- ⚠️ "Irish Morning"（凯尔特，欢快，竖琴）← 缺少小提琴
- ⚠️ "Vivaldi - Spring"（古典，欢快，小提琴）← 不是凯尔特
- ❌ "Celtic Night"（凯尔特，忧伤，风笛）← 不欢快，无小提琴

### 阶段2：LLM 精排（分析后）
LLM 会对每首歌打分：
```json
{
  "matched_songs": [
    {
      "song_id": 123,
      "song_name": "Celtic Dreams",
      "match_score": 0.95,
      "match_reason": "典型的凯尔特风格，情绪欢快，小提琴旋律贯穿全曲，完美符合用户要求"
    },
    {
      "song_id": 456,
      "song_name": "Irish Morning",
      "match_score": 0.75,
      "match_reason": "凯尔特风格，情绪欢快，但主要乐器是竖琴而非小提琴"
    }
  ]
}
```

---

## 注意事项

1. **music_features 表的作用**：
   - ⚠️ 该表**不是必需的**，当前方案可以不使用该表
   - 如果生成该表，也**不应该用 LLM 生成**
   - 如有需要，可用规则匹配或简单机器学习模型生成

2. **Embedding 模型 ≠ LLM**：
   - Embedding 模型：将文本转为向量（如 MiniMax Embedding API）
   - LLM：大语言模型，理解复杂语义（如 MiniMax Chat API）
   - 当前方案：离线用 Embedding，实时精排用 LLM

3. **成本对比**：
   - Embedding：每首约 $0.0001，100 万首约 $100
   - LLM 生成特征：每首约 $0.01，100 万首约 $10,000
   - LLM 精排 200 首：每次约 $0.02，可接受

---

## 结论

**核心原则**：
- ❌ 不在离线阶段使用 LLM 生成 music_feature
- ✅ 仅在实时查询的精排阶段使用 LLM
- ✅ 粗召回阶段用向量数据库（Embedding）
- ✅ 成本可控、延迟可接受、效果有保证

**适用场景**：
- 基本查询（歌名、歌手）→ SQL ILIKE
- 语义查询（"适合工作的音乐"）→ 向量检索
- 复杂查询（"欢快的凯尔特音乐，有小提琴伴奏"）→ 向量召回 + LLM 精排

---

## 更新位置

`design.md` 第 408-572 行，新增 **3.0.3 节：复杂查询的完整搜索流程（示例）**

查看命令：
```bash
sed -n '408,572p' /home/luke/code_project/musicRecommend/design.md
```
